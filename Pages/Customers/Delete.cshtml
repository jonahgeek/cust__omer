@page
@model Cust__omer.Pages.Customers.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <div class="row mt-5">
        <div class="offset-md-4 col-md-5">
            <div class="card border-danger">
                <div class="card-body">
                    <strong>Are you sure you want to delete @Html.DisplayFor(model => model.Customer.FirstName) ?</strong> <br/>
                    <p>
                        <form method="post">
                            <input type="hidden" asp-for="Customer.Id" />
                            <input type="submit" value="Delete" class="btn btn-danger" />
                        </form>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
